t = (1,2,3,1)
# một tuple t với các phần tử là 1, 2, 3 và 1

count = t.count(1)
# sử dụng phương thức count() để đếm số lần xuất hiện của giá trị 1 trong tuple t
# gán kết quả vào biến count

index = t.index(2)
# sử dụng phương thức index() để tìm vị trí đầu tiên của giá trị 2 trong tuple t
# gán kết quả vào biến index

print(count)
# in ra số lần xuất hiện của giá trị 1 trong tuple t

print(index)
# in ra vị trí đầu tiên của giá trị 2 trong tuple t
