var1 = (1 + 2) * 5   
# kết quả của (1 + 2) * 5, tức là: 15.
# Biểu thức này được tính trước rồi kết quả là một số nguyên, và sau đó được gán cho var1!

print(type(var1), " = ", var1)
# Đoạn mã trên dùng để hiển thị kiểu dữ liệu của biến var1 tại câu lệnh này
# trong trường hợp này type(var1) sẽ trả về kiểu 'int', tức là var1 là một số nguyên và giá trị của var1 là 15.

var2 = ()
# Biến var2 được gán rỗng tại đây. Trong Python, dãy rỗng hoặc không được sử dụng để tạo tuple khi chúng chứa một phần tử
# mà nó chỉ được sử dụng để định nghĩa biểu thức hoặc phép biến đổi. Do đó, dãy có rỗng đơn, var2 () là một số nguyên, không phải là một tuple.

print(type(var2), " ", var2)
# type(var2) sẽ trả về class 'int', tức là var2 là một số nguyên.
